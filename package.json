{
  "name": "7.6",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "ChromeHeadless": "npx cypress run --browser chrome --headless",
    "ChromeMobile": "npx cypress run --browser chrome --headed --config-file cypress.env.js",
    "FirefoxHeadless": "npx cypress run --browser firefox --headless",
    "FirefoxMobile": "npx cypress run --browser firefox --headed --config-file cypress.env.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^11.2.0"
  }
}
